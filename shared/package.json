{
  "name": "@ems/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared, framework-agnostic TypeScript contract (DTOs, enums) for client and server",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "employee-project-mgmt": "file:.."
  }
}
